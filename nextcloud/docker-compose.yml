version: '2'
services:
  nextcloud:
    image: nextcloud:22
    hostname: nextcloud
    container_name: nextcloud
    network_mode: "bridge"
    volumes:
      - "nextcloud:/var/www/html"

volumes:
  nextcloud:
    external: true
